{
  "name": "xazonnox-unified",
  "version": "1.0.0",
  "description": "Unified single-domain platform (API, Admin, and Storefront Theme)",
  "private": true,
  "scripts": {
    "build:api": "cd apix && npm run build",
    "build:admin": "cd adminx && npm run build",
    "build:theme": "cd themex && npm run build",
    "build": "npm run build:api && npm run build:admin && npm run build:theme",
    "start:api": "cd apix && node dist/main.js",
    "start:theme": "cd themex && node dist/angular-ui/server/server.mjs",
    "start": "node start-unified.js"
  }
}
